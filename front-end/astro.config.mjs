import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://jose-kauan-pereira00.github.io', 
  base: '/ToDoAndNotifications', // <--- Exatamente igual ao nome no seu link do GitHub
});
